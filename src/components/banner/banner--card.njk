<cor-banner
  type="card"
  image="{{ image }}" 
  title="{{ title }}" 
  {% if description %}
  description="{{ description }}"
  {% endif %}
  link="{{ link }}">
</cor-banner>

<script type="module" src='../banner/cor-banner.js'></script>
